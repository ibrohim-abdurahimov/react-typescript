export interface LoginForm {
  username: string,
  password: string
}

export interface Props { 
  title: string,
  price: number,
}

export interface Product {
    id: number,
    image: string,
    name: string,
}